// ─── Punto de entrada principal ───

document.addEventListener('DOMContentLoaded', () => {
  initCanvas();
  bindUI();
  setStatus('Bienvenido a CiSIPro. Arrastra componentes al canvas para comenzar.');
});